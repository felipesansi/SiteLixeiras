@{
    ViewData["Title"] = "Confirmação de Pedido";
}
<div class="container mt-4">
    <div class="alert alert-success text-center">
        <h4 class="mb-3">🎉 Pedido realizado com sucesso!</h4>
        <p>@ViewBag.Mensagem</p>
        <a class="btn btn-primary mt-3" href="@Url.Action("Index", "Home")">Voltar para a loja</a>
    </div>
</div>
