@model SiteLixeiras.Models.Pedido

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <title>Confirmação de Pedido</title>
    <style>
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: #f8f9fa;
            color: #222;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px #eee;
            padding: 32px;
        }

        .header {
            background: #1A3300;
            color: #fff;
            padding: 16px;
            border-radius: 8px 8px 0 0;
            text-align: center;
        }

        .btn {
            display: inline-block;
            background: #1A3300;
            color: #fff;
            padding: 10px 24px;
            border-radius: 4px;
            text-decoration: none;
            margin-top: 16px;
        }

        .pedido-info {
            margin: 24px 0;
        }

            .pedido-info th, .pedido-info td {
                padding: 6px 8px;
            }

        .footer {
            color: #888;
            font-size: 13px;
            margin-top: 32px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Pedido Recebido!</h2>
        </div>
        <p>Olá @Model.Usuario?.UserName,</p>
        <p>Seu pedido <strong>#@Model.PedidoId</strong> foi criado com sucesso em <strong>@Model.PedidoEnviado.ToString("dd/MM/yyyy HH:mm")</strong>.</p>
        <p>Resumo do pedido:</p>
        <table class="pedido-info" width="100%">
            <tr>
                <th align="left">Total:</th>
                <td>R$ @Model.PedidoTotal.ToString("N2")</td>
            </tr>
            <tr>
                <th align="left">Itens:</th>
                <td>@Model.TotalItensPedidos</td>
            </tr>
            <tr>
                <th align="left">Endereço de entrega:</th>
                <td>
                    @Model.EnderecoEntrega?.Rua, @Model.EnderecoEntrega?.Numero<br />
                    @Model.EnderecoEntrega?.Bairro, @Model.EnderecoEntrega?.Cidade - @Model.EnderecoEntrega?.Estado<br />
                    CEP: @Model.EnderecoEntrega?.Cep
                </td>
            </tr>
        </table>
        <p>Você pode acompanhar o status do seu pedido acessando sua conta no site.</p>
    
        <div class="footer">
            <p>Obrigado por comprar conosco!<br />Equipe Lixeiras de resina</p>
        </div>
    </div>
</body>
</html>